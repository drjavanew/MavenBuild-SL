name: CICD using Maven and tomcat
on:
 push:  # run when there is commit to repo
 workflow_dispatch:  # run manually
jobs:
 CICDjob:
   runs-on: ubuntu-latest
   steps:
    # - name: Update dependency graph
    #   uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
    - name: Connect to Lab machine and Deploy the code on tomcat9 
      uses: cross-the-world/ssh-scp-ssh-pipelines@latest
      with:
        host: '3.108.227.224'
        user: 'labuser'
        pass: 'Nuvelabs123$'
        port: 22
        connect_timeout: 10s   
        scp: |
          './target/*war' => /tmp/
        last_ssh: |
          ls -lart /temp

    - name: Invoke Jenkins Deployment Job
      uses: appleboy/jenkins-action@master
      with:
        host: "http://172-31-42-56:8080"
        user: "admin"
        pass: ${{ secrets.JENKINS_TOKEN }}
        job: "Test_GitHubActions_job"
